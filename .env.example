# Environment Variables Template
# Copy this file to secrets/influxdb.env and secrets/tibber.env
# Replace placeholder values with your actual secrets

# ============================================
# InfluxDB Secrets (REQUIRED - secrets/influxdb.env)
# ============================================
INFLUX_TOKEN=your_influxdb_api_token_here
INFLUX_ORG=your_influxdb_org_id_here

# How to get these:
# 1. Log into InfluxDB UI: http://your-nas-ip:8086
# 2. Go to Data > API Tokens
# 3. Generate "All Access Token"
# 4. Copy token value and org ID

# ============================================
# Tibber Secrets (OPTIONAL - secrets/tibber.env)
# ============================================
TIBBER_API_TOKEN=your_tibber_api_token_here

# How to get this:
# 1. Go to https://developer.tibber.com/
# 2. Log in with your Tibber account
# 3. Create a Personal Access Token
# 4. Copy the token value
#
# Note: Tibber ingestion will be skipped if this is not set

# ============================================
# Dagster PostgreSQL Storage (OPTIONAL)
# ============================================
# By default, Dagster uses SQLite for local development
# For production, configure PostgreSQL for better performance:

#DAGSTER_POSTGRES_USER=dagster
#DAGSTER_POSTGRES_PASSWORD=your_secure_password_here
#DAGSTER_POSTGRES_DB=dagster
#DAGSTER_POSTGRES_HOST=localhost
#DAGSTER_POSTGRES_PORT=5432

# How to set up PostgreSQL for Dagster:
# 1. Install PostgreSQL on your system
# 2. Create database: createdb dagster
# 3. Create user: createuser dagster -P
# 4. Grant permissions: GRANT ALL PRIVILEGES ON DATABASE dagster TO dagster;
# 5. Uncomment the variables above and fill in your values
#
# Note: All 5 PostgreSQL variables must be set together or Dagster falls back to SQLite

# ============================================
# Gas Conversion Parameters (OPTIONAL)
# ============================================
# Override default gas conversion values from config.yaml if needed:

#GAS_ENERGY_CONTENT=11.504  # kWh per m³ (default: 11.504)
#GAS_Z_FACTOR=0.8885        # Gas compression factor (default: 0.8885)

# These values are typically provided by your gas supplier
# Check your gas bill or contact your supplier for accurate values

# ============================================
# IMPORTANT SECURITY NOTES
# ============================================
# - NEVER commit actual secrets to version control
# - Store secrets in secrets/*.env files (they are .gitignored)
# - Rotate secrets immediately if compromised
# - Use restrictive permissions: chmod 600 secrets/*.env
# - For production deployments, consider using secret management tools:
#   * HashiCorp Vault
#   * AWS Secrets Manager
#   * Kubernetes Secrets
#   * Docker Secrets
