# Configuration Database (PostgreSQL)
# This database stores editable configuration (meters, households, settings)
CONFIG_DATABASE_URL=postgresql://dagster:dagster@localhost:5432/nebenkosten_config

# InfluxDB Configuration
# This database stores time-series data (meter readings, consumption)
INFLUX_URL=http://192.168.1.75:8086
INFLUX_TOKEN=your_token_here
INFLUX_ORG=lampfi
INFLUX_BUCKET_RAW=lampfi
INFLUX_BUCKET_PROCESSED=lampfi_processed

# Gas Conversion Parameters (for unit conversions)
# NOTE: These are now stored in the config database but kept here as fallback
GAS_ENERGY_CONTENT=11.504
GAS_Z_FACTOR=0.8885
